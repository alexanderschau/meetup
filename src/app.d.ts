type MeetupType = {
  title: string;
  description?: string;
  url?: string;
  from: number;
  to: number;
};
type MeetupMateType = {
  tz: string;
};

type SEODataType = {
  title: string;
  description: string;
  imgUrl: string;
  url: string;
};
